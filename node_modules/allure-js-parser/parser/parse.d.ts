import { AllureContainer, AllureNode, AllureTest } from '../types';
/**
 * Populate test results (without parent)
 * @param allureResultsPath - path to allure-results folder
 */
export declare const populateTestResults: (allureResultsPath: string) => AllureTest[];
/**
 * Populate containers results (without parents)
 * @param allureResultsPath - path to allure-results folder
 */
export declare const populateContainers: (allureResultsPath: string) => AllureContainer[];
/**
 * Find parent within containers
 * @param containers
 * @param target
 */
export declare function populateAllParents(containers: AllureContainer[], target: AllureNode): void;
export declare const parseAllure: (directoryPath: string, config?: {
    failOnError?: boolean;
}) => AllureTest[];
